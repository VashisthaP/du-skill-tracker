# =====================================================
# SkillHive - Environment Variables Configuration
# Copy this file to .env and fill in your values
# =====================================================

# ---------- Flask Configuration ----------
FLASK_ENV=development
# Generate a strong secret key for production: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=dev-secret-key-change-in-production

# ---------- Database Configuration ----------
# For LOCAL DEVELOPMENT (SQLite - no setup needed):
DATABASE_URL=sqlite:///skillhive.db
# For PRODUCTION (PostgreSQL on Azure):
# DATABASE_URL=postgresql://adminuser:YourPassword@your-server.postgres.database.azure.com:5432/skillhive?sslmode=require

# ---------- Azure AD / Entra ID Configuration ----------
# Required for production SSO login
# Get these from Azure Portal > App Registrations > Your App
AZURE_AD_CLIENT_ID=your-azure-ad-app-client-id
AZURE_AD_CLIENT_SECRET=your-azure-ad-app-client-secret
AZURE_AD_TENANT_ID=your-azure-ad-tenant-id
# For local dev: http://localhost:5000/auth/callback
# For production: https://your-app.azurewebsites.net/auth/callback
AZURE_AD_REDIRECT_URI=http://localhost:5000/auth/callback

# ---------- Email Configuration (Office 365 SMTP) ----------
# Used to send notifications to evaluators and demand raisers
MAIL_SERVER=smtp.office365.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=your-email@accenture.com
MAIL_PASSWORD=your-email-app-password
MAIL_DEFAULT_SENDER=SkillHive Portal <your-email@accenture.com>

# ---------- Azure Blob Storage (Resume Uploads) ----------
# Get from Azure Portal > Storage Account > Access Keys
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=...;AccountKey=...;EndpointSuffix=core.windows.net
AZURE_STORAGE_CONTAINER=resumes

# ---------- Application Insights (Monitoring) ----------
# Get from Azure Portal > Application Insights > Connection String
APPINSIGHTS_INSTRUMENTATIONKEY=your-instrumentation-key

# ---------- Development Mode ----------
# Set to 'true' to enable local development without Azure AD
# This creates a mock login system for testing
DEV_MODE=true
